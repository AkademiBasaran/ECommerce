@model IEnumerable<Producer>;
@{
    ViewData["Title"] = "Aktör Listesi";
}


<div class="row">
    <div class="col-md-8 offset-md-2">

        <table class="table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(m => m.ProfilePictureUrl)</th>
                    <th>@Html.DisplayNameFor(m => m.FullName)</th>
                    <th>@Html.DisplayNameFor(m => m.Bio)</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var producer in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="rounded-circle"
                                 src="@producer.ProfilePictureUrl"
                                 alt="@producer.FullName"
                                 style="max-width=100px" />
                        </td>
                        <td class="align-middle">@Html.DisplayFor(m => producer.FullName)</td>
                        <td class="align-middle">@Html.DisplayFor(m => producer.Bio)</td>
                        <td class="align-middle">

                            <a class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i></a>|
                            <a class="btn btn-outline-info"><i class="bi bi-eye"></i></a>|
                            <a class="btn btn-danger"><i class="bi bi-trash"></i></a>

                        </td>
                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>



